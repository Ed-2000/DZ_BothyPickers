using UnityEngine;
using Unity.AI.Navigation;

public class Marker : MonoBehaviour
{
    private void OnTriggerEnter(Collider other)
    {
        //if (other.TryGetComponent(out Bot bot) && bot.)
        //{

        //}
    }
}
